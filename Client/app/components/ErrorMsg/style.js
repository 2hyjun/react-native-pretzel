import React from 'react';
import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    wrong: {
        flex: 0.4,
        alignItems: 'center',
        justifyContent: 'center',
        marginTop: 10,
    },
    wrongTxt: {
        color: 'red',
        fontWeight: 'bold'
    },
});

export default styles;